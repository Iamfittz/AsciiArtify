apiVersion: apps/v1
kind: Deployment
metadata:
  name: asciiartify
spec:
  replicas: 1
  selector:
    matchLabels:
      app: asciiartify
  template:
    metadata:
      labels:
        app: asciiartify
    spec:
      containers:
        - name: asciiartify
          image: nginx:latest
          #image: nginx:1.27
          ports:
            - containerPort: 8080
